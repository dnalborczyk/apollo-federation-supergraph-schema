{
  "name": "schema-users",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "copy": "copyfiles --up 1 src/users.{json*,graphql} dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "sideEffects": false,
  "author": "",
  "license": "MIT",
  "dependencies": {
    "graphql": "15.7.2"
  }
}
